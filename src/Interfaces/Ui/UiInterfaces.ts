export namespace UiInterfaces {
    export namespace Widths {
        export interface Merge {
            a_xs?: string|number;
            b_sm?: string|number;
            c_md?: string|number;
            d_lg?: string|number;
            e_xl?: string|number;
        }

        export interface All {
            a_xs: string|number;
            b_sm: string|number;
            c_md: string|number;
            d_lg: string|number;
            e_xl: string|number;
        }
    }
}
